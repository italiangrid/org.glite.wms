<?xml version="1.0" encoding="UTF-8" ?>
<!--
	Copyright (c) 2004 on behalf of the EU EGEE Project: 
	The European Organization for Nuclear Research (CERN), 
	Istituto Nazionale di Fisica Nucleare (INFN), Italy
	Datamat Spa, Italy
	Centre National de la Recherche Scientifique (CNRS), France
	CS Systeme d'Information (CSSI), France
	Royal Institute of Technology, Center for Parallel Computers (KTH-PDC), Sweden
	Universiteit van Amsterdam (UvA), Netherlands
	University of Helsinki (UH.HIP), Finland
	University of Bergen (UiB), Norway
	Council for the Central Laboratory of the Research Councils (CCLRC), United Kingdom

	Build file for the EGEE Middleware Test 3 WMS Subsystem
	
	Authors: Alberto Di Meglio <alberto.di.meglio@cern.ch>		
	Version info: $Id$
	Release: $Name$

	Revision history:
	$Log$
	Revision 1.2  2004/04/30 16:22:06  eronchie
	Added stage target
	Added stage, distsrc, and distbin targets
	Added DISTBIN, and WORKDIR

	Revision 1.1  2004/04/29 01:36:29  dimeglio
	Added org.egee.test3.wms
	
-->

<project name="test3-wms" default="export">

	<description>
		Ant build file to build the EGEE Test 3 WMS
	</description>
	
	<!-- Import properties -->
	<import file="../org.egee/project/properties.xml" />
	<import file="${workspace.dir}/${test3.wms.subsystem}/project/properties.xml" />

	<!-- Load dependencies files -->
	<property file="${dependencies.file}"/>
	<property file="${subsystem.dependencies.file}"/>

	<!-- Load version file -->
	<property file="${version.file}"/>

	<!-- ===============================================
		 Public common targets
	     =============================================== -->

	<target name="envinfo">
		<!-- Workspace paths -->
		<echo>*****************************************************************</echo>
		<echo>*****************************************************************</echo>
		<echo>****** ${subsystem.name}	</echo>
		<echo>*****************************************************************</echo>
		<echo>*****************************************************************</echo>
		<echo>  </echo>
		<echo>Master project path:	${master.dir}</echo>
		<echo>Master project name:	${application.name}</echo>
		<echo>Subsystem name:		${subsystem.name}</echo>
	</target>

	<target name="envcheck" unless="checks.done"
		description="Check environment configuration">

		<antcall target="envinfo" />
		<antcall target="defaultenvchecks" />
	</target>

	<!-- Default envchecks (defined in envchecks.xml) -->
	<import file="${master.project.dir}/envchecks.xml" />


	<target name="localinit" depends="envcheck">
	</target>

	<target name="init" depends="localinit">
		<antcall target="buildmodules">
			<param name="target" value="init"/>
		</antcall>
	</target>

	<target name="checkstyle" depends="localinit">
		<antcall target="buildmodules">
			<param name="target" value="checkstyle"/>
		</antcall>
	</target>

	<target name="compile" depends="localinit">
		<antcall target="buildmodules">
			<param name="target" value="compile"/>
		</antcall>
	</target>

	<target name="compiletest" depends="localinit">
		<antcall target="buildmodules">
			<param name="target" value="compiletest"/>
		</antcall>
	</target>

	<target name="unittest" depends="localinit">
		<antcall target="buildmodules">
			<param name="target" value="unittest"/>
		</antcall>
	</target>

	<target name="unitcoverage" depends="localinit">
		<antcall target="buildmodules">
			<param name="target" value="unitcoverage"/>
		</antcall>
	</target>

	<target name="export" depends="localinit">
		<antcall target="buildmodules">
			<param name="target" value="export"/>
		</antcall>
	</target>

        <target name="stage" depends="localinit">
                <antcall target="buildmodules">
                        <param name="target" value="stage"/>
                </antcall>
        </target>


	<target name="dist" depends="localinit">
		<antcall target="buildmodules">
			<param name="target" value="dist"/>
		</antcall>
	</target>

	<target name="install" depends="localinit">
		<antcall target="buildmodules">
			<param name="target" value="install"/>
		</antcall>
	</target>

	<target name="doc" depends="localinit">
		<antcall target="buildmodules">
			<param name="target" value="doc"/>
		</antcall>
	</target>

	<target name="tag" depends="clean">

		<!-- This target is not recursive
			 It is only used to set private tags
			 Tagging is done by the build system -->
		<!-- TODO -->		

	</target>

	<target name="clean">

		<property name="offline.repository" value="true" />
		<antcall target="buildmodules">
			<param name="target" value="clean"/>
		</antcall>

		<delete dir="${bin.dir}" />
		<delete dir="${lib.dir}" />
		<delete dir="${autosrc.dir}" />
		<delete dir="${autodoc.dir}" />
		<delete dir="${test.reports.dir}" />

	</target>

	<!-- ===============================================
		 Private targets
	     =============================================== -->



	<!-- ===============================================
		 External dependencies
	     =============================================== -->
	
	<!-- Import external dependencies targets -->
	<import file="${master.project.dir}/external-dependencies.xml" />

	<!-- ===============================================
		 Modules proxy targets
	     =============================================== -->

	<target name="globus_ssl_utils">
		<ant dir="${master.dir}/../${subsystem.name}.thirdparty-globus_ssl_utils" target="${target}" />
	</target>

	<target name="bypass">
		<ant dir="${master.dir}/../${subsystem.name}.thirdparty-bypass" target="${target}" />
	</target>

	<target name="globus_gridftp_server" depends="globus">
		<ant dir="${master.dir}/../${subsystem.name}.thirdparty-globus_gridftp_server" target="${target}" />
	</target>

	<target name="loki">
		<ant dir="${master.dir}/../${subsystem.name}.thirdparty-loki" target="${target}" />
	</target>

	<target name="common" depends="boost,classads,globus">
		<ant dir="${master.dir}/../${subsystem.name}.common" target="${target}" />
	</target>

	<!-- Main proxy -->
	<target name="buildmodules" depends="globus_ssl_utils,
									     bypass,
										 globus_gridftp_server,
										 loki,
										 common" />
		
</project>
