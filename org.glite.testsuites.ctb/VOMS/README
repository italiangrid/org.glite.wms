The script myCheck-VOMS can be used to run the VOMS tests without SAM. To get a list of options type

./myCheck-VOMS -h

The following test lists are available:

Tests for voms-admin command:		test-sequence.lst.voms-admin
Tests for voms-proxy-init command:	test-sequence.lst.voms-core
Both lists:				test-sequence.lst.full

You need a valid VOMS proxy to run the tests. For the admin tests the VOMS server has to give the global ACL "Allow all" to the user running the tests.
