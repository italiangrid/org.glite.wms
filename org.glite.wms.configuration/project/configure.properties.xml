	<!-- ======================================================
                 Define extra properties here ...
                 ====================================================== -->
        
	<project name="configure options">                                                                        
        <property name="configure.args"
               value="--prefix=${workspace.abs.dir}/stage --with-condor-prefix=${with.condor.prefix} --with-glite-location=${with.glite.location} --with-dist-location=${with.dist.location}"/>

	</project>
