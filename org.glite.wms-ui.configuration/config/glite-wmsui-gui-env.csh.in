#!/bin/csh

if( ! $?COG_INSTALL_PATH ) then
    setenv COG_INSTALL_PATH "@COG_INSTALL_PATH@"
endif

if( ! $?CLASSADJ_INSTALL_PATH ) then
   setenv CLASSADJ_INSTALL_PATH "@CLASSADJ_INSTALL_PATH@"
endif

if( ! $?JAVA_INSTALL_PATH ) then
    setenv JAVA_INSTALL_PATH "@JAVA_INSTALL_PATH@"
endif

if( ! $?LOG4J_INSTALL_PATH ) then
    setenv LOG4J_INSTALL_PATH "@LOG4J_INSTALL_PATH@"
endif

