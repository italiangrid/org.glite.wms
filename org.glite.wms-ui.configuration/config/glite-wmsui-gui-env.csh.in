#!/bin/csh

if( ! $?JGLOBUS_INSTALL_PATH ) then
    setenv JGLOBUS_INSTALL_PATH "@JGLOBUS_INSTALL_PATH@"
endif

if( ! $?JCLASSAD_INSTALL_PATH ) then
   setenv JCLASSAD_INSTALL_PATH "@JCLASSAD_INSTALL_PATH@"
endif

if( ! $?JAVA_INSTALL_PATH ) then
    setenv JAVA_INSTALL_PATH "@JAVA_INSTALL_PATH@"
endif

if( ! $?LOG4J_INSTALL_PATH ) then
    setenv LOG4J_INSTALL_PATH "@LOG4J_INSTALL_PATH@"
endif

