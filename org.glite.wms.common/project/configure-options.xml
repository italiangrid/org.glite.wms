	<!-- ======================================================
                 Define extra properties here ...
                 ====================================================== -->
        
	<project name="configure options">                                                                        
        <property name="configure.args"
               value="--with-globus-prefix=${with.globus.prefix} --with-globus-thr-flavor=${with.globus.thr.flavor} --with-globus-nothr-flavor=${with.globus.nothr.flavor} --with-classads-prefix=${with.classads.prefix} --with-boost-prefix=${with.boost.prefix} --enable-boost_debug=${enable.boost.debug}"/>

	</project>
