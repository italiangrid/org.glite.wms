	<!-- ======================================================
                 Define extra properties here ...
                 ====================================================== -->

        <project name="configure options">
        <property name="configure.args"
               value="--prefix=${workspace.abs.dir}/stage --with-boost-prefix=${with.boost.prefix} --enable-boost_debug=${enable.boost.debug} --with-cppunit-prefix=${with.cppunit.prefix} --with-replica-prefix=${with.replica.prefix} --with-glite-location=${with.glite.location} --with-dist-location=${with.dist.location}"/>

        </project>
